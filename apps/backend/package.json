{
  "name": "backend",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "debug": "node scripts/debug-extraction.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^24.10.15",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.69.0"
  }
}
